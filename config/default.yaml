paths:
  raw_data: ./data/dales_las
  model_data: ./data/dales_blocks
  logs: ./logs
  figures: ./figs
  models: ./model_objects

data_preprocessing:
  block_size: 50.0                   # meters
  stride: 25.0                       # meters (50% overlap)
  num_points: 4096                   # PointNet input size
  min_points_in_block: 1024          # skip tiny blocks

  # Optional: cap blocks per tile for quick experiments (None = keep all)
  max_blocks_per_tile: None

model: 
  model_name: pointnet
  num_classes: 5
  num_channels: 3

training:
  num_points: 1024 # TODO: do we keep two?
  batch_size: 32
  num_epochs: 1
  learning_rate: 0.001
  dropout_rate: 0.3
  optimizer: adam

# train_test_split:
#   train_ratio: 0.8
#   test_ratio: 0.2